// import ReactMarkdown from 'react-markdown';
// import remarkGfm from 'remark-gfm'; // Optional: For GitHub-Flavored Markdown (tables, strikethrough, etc.)
// import rehypeRaw from 'rehype-raw'; // Optional: To render HTML tags in markdown (ensure input is sanitized)

// function PostContent({ content }) {
//   return (
//     <div className="content prose max-w-none">
//       <ReactMarkdown
//         children={content}
//         remarkPlugins={[remarkGfm]} // Optional: Enables GitHub-Flavored Markdown
//         rehypePlugins={[rehypeRaw]} // Optional: Enables raw HTML rendering
//       />
//     </div>
//   );
// }

// export default PostContent;